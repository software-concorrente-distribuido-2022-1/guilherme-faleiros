Para criar o banco de dados, é possível fazê-lo com docker, através do docker-compose contido no projeto:
```
docker-compose up -d
```

Logo após, executar os arquivos contidos em */src/main/resources/database.sql*
